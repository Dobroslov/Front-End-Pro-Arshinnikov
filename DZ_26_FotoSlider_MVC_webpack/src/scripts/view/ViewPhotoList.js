const albumItemTemplate =
  document.getElementById('albumItemTemplate').innerHTML;
const photoItemTemplate =
  document.getElementById('photoItemTemplate').innerHTML;

class ViewPhotoList {
  constructor(photos) {    
    this._photosListContainer = photos;
    
  }
}
